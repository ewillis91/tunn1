// import React from 'react';
// import { render } from '@testing-library/react-native';
// import App from '../App'; 

// describe('App Component', () => {
//   it('renders correctly', () => {
//     const { getByTestId} = render(<App />); // Render your App component
    
//     // Use queries to check if specific elements are rendered
//     const SplashScreen = getByTestId('splash-logo'); 

//     // Make assertions to verify that the expected elements are present
//     expect(SplashScreen).toBeTruthy();
//   });
// });
